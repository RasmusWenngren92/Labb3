/*
  * This is a template file for database connection settings
  * 1. Replace the values below with your local database settings:
  *   - YOUR_SERVER: Usually (localdb)\MSSQLLocalDB for local development
  *   - YOUR_DATABASE: The name of your database (e.g., SchoolDB)
  * 2. Change filename to appsettings.json
*/

{
  "ConnectionStrings": {
    "DefaultConnection": "Server=YOUR_SERVER;Database=YOUR_DATABASE;Trusted_Connection=True;TrustServerCertificate=True;"
  }
}
